#ifndef RENUMS_H_INCLUDED
#define RENUMS_H_INCLUDED

/**< Defines enumerators for convenience, each wrapped within a namespace. */

namespace RMenuType {
  enum MenuType {
    DEFAULT = 0,
    MAINMENU = 1
  };
}

#endif // RENUMS_H_INCLUDED
